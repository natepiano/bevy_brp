# Configure independent releases for workspace packages
allow-branch = ["main"]
consolidate-commits = false
shared-version = false

# Tag format for each package
tag-prefix = "{{crate_name}}-"
tag-name = "{{prefix}}v{{version}}"